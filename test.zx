# Auto-typed (dynamic)
nama = "Budi"                # string
umur = 25                    # int
berat = 70.5                 # float
active = true                # bool
tags = ["cli", "tools"]      # array
config = {host: "localhost"} # object

# Constant (gak bisa diubah)
const VERSION = "1.0.0"

# Multi-assig
x, y = 10, 20
a, b, c = 1, 2.5, "tiga"

# Array indexing & CLI args
first_tag = tags[0]
second_tag = tags[1]
first_arg = args[0]
