# Auto-typed (dynamic)
nama = "Budi"                # string
umur = 25                    # int
berat = 70.5                 # float
active = true                # bool
tags = ["cli", "tools"]      # array
config = {host: "localhost"} # object

# Constant (gak bisa diubah)
const VERSION = "1.0.0"

# Multi-assign
x, y = 10, 20
a, b, c = 1, 2.5, "tiga"

# Array indexing & CLI args
first_tag = tags[0]
second_tag = tags[1]
first_arg = args[0]

# Print statements
print("=== TES BUILT-IN PRINT ===")
print("Halo, nama saya", nama, "dan umur saya", umur)
print("Tags:", tags)
print("Tag pertama:", first_tag)
print("CLI Arg pertama:", first_arg)
